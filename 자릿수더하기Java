public class Solution {
    public int solution(int n) {
        int sum = 0; //함수를 구하기 위한 변수 선언
        while (n>0) {
            sum += n % 10; //입력값을 10으로 나눈 나머지를 sum에 더한다.
            n /= 10; //입력값을 10으로 나눈다.
        }

        return sum;
    }
}